#!/usr/bin/env bash

set -o errexit

echo 'TODO: Make this work'

# scriptdir="$(dirname "$0")"
# cd "$scriptdir/.."

# echo 'Checking if app-building images are available...'

# cd "./devops"

# docker-compose exec app python manage.py migrate
# docker-compose exec app python manage.py copy_media
